/**
 * Replace {key} placeholders in a URL with values from params
 * Alias for buildUrl for compatibility
 */
import buildUrl from "./buildUrl";

export const resolvePath = buildUrl;
export default buildUrl;
